clang++ -fPIC -shared MultiInstOpts.cpp -o libMultiInstOpts.so `llvm-config --cxxflags --ldflags --system-libs --libs core passes`

opt -load-pass-plugin ./libMultiInstOpts.so -passes="multi-inst-opt,dce" -S test/test.ll -o test/output.ll


